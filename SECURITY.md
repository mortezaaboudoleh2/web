# سیاست امنیتی

## نسخه‌های پشتیبانی‌شده

| نسخه | پشتیبانی |
|---|---|
| 1.x | ✅ |

## گزارش آسیب‌پذیری

اگر آسیب‌پذیری امنیتی پیدا کردید، **لطفاً آن را به‌صورت عمومی در Issue مطرح نکنید.**

به‌جای آن از بخش [Security Advisories](https://github.com/USERNAME/hana-love/security/advisories/new) گیت‌هاب استفاده کنید.

در گزارش خود شامل کنید:
- توضیح آسیب‌پذیری
- مراحل بازتولید
- تأثیر احتمالی
- پیشنهاد رفع (در صورت وجود)

پاسخ اولیه معمولاً ظرف ۷۲ ساعت ارسال می‌شود.

## توصیه‌های استقرار امن

- `ADMIN_PASS` را از مقدار پیش‌فرض تغییر دهید و از رمز قوی استفاده کنید.
- همیشه پشت HTTPS اجرا کنید.
- فایل `.env` را کامیت نکنید.
- دسترسی به `DATA_DIR` را محدود کنید؛ حاوی اطلاعات شخصی است.
- برای محیط عمومی، محدودسازی نرخ (rate limiting) روی `/api/admin/login` اضافه کنید.
